const state = () => ({
  user: null
});

// getters
const getters = {
  user: (state) => {
    return state.user;
  }
};

// actions
const actions = {
  
};

// mutations
const mutations = {
  
};

export default {
  namespaced: true,
  state,
  getters,
  actions,
  mutations
};
